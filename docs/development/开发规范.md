# 智能投标预审智能体 开发规范文档

## 1. 开发概述

### 1.1 开发环境搭建

```bash
# 克隆项目
git clone <repository-url>
cd ai-shencha

# 安装依赖
npm install

# 配置环境变量
cp .env.example .env

# 初始化数据库
npm run db:push

# 启动开发服务器
npm run dev
```

### 1.2 开发流程

```
需求分析 → 设计方案 → 编码实现 → 测试验证 → 提交PR → Code Review → 合并发布
```

---

## 2. Git 规范

### 2.1 分支命名规范

| 类型 | 格式 | 示例 |
|------|------|------|
| **功能分支** | `feature/xxx` | `feature/document-upload` |
| **修复分支** | `fix/xxx` | `fix/auth-redirect` |
| **重构分支** | `refactor/xxx` | `refactor/api-response` |
| **文档分支** | `docs/xxx` | `docs/api-documentation` |
| **发布分支** | `release/vX.X.X` | `release/v1.0.0` |

### 2.2 Commit 规范

使用 Conventional Commits 格式：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型说明**：

| 类型 | 说明 |
|------|------|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `docs` | 文档更新 |
| `style` | 代码格式（不影响逻辑） |
| `refactor` | 重构 |
| `perf` | 性能优化 |
| `test` | 测试相关 |
| `chore` | 构建/工具变更 |

**示例**：
```
feat(api): add document parse status endpoint

Add GET /api/documents/[id]/parse endpoint to check MinerU task status.
Support polling for async parse progress.

Closes #123
```

### 2.3 PR 流程

1. 创建功能分支
2. 完成开发和测试
3. 提交 Pull Request
4. 等待 Code Review
5. 修复 Review 问题
6. 合并到主分支

**PR 标题格式**：`feat: xxx` / `fix: xxx` / `docs: xxx`

### 2.4 Code Review 要求

- 代码逻辑正确
- 符合开发规范
- 无明显性能问题
- 无安全漏洞
- 有必要的测试

---

## 3. 代码风格

### 3.1 TypeScript 规范

**命名规范**：

| 类型 | 规范 | 示例 |
|------|------|------|
| **变量** | camelCase | `documentId`, `parseStatus` |
| **常量** | UPPER_SNAKE_CASE | `MAX_FILE_SIZE`, `DEFAULT_TIMEOUT` |
| **函数** | camelCase | `getDocumentById`, `parseDocument` |
| **类** | PascalCase | `DocumentParser`, `MinerUClient` |
| **接口** | PascalCase + I前缀（可选） | `IDocument`, `ParseResult` |
| **类型** | PascalCase | `DocumentType`, `ParseStatus` |
| **枚举** | PascalCase | `ProjectStatus`, `DocType` |
| **文件** | kebab-case | `document-parser.ts`, `mineru-client.ts` |

**类型定义**：

```typescript
// 优先使用 interface 定义对象
interface Document {
  id: string;
  name: string;
  parseStatus: ParseStatus;
}

// 使用 type 定义联合类型、工具类型
type ParseStatus = 'pending' | 'processing' | 'completed' | 'failed';
type DocumentWithBlocks = Document & { blocks: DocumentBlock[] };
```

**导入规范**：

```typescript
// 使用路径别名
import { db } from '@/lib/db/client';
import { Button } from '@/components/ui/button';

// 按类型分组导入
import { useState, useEffect } from 'react';  // React
import { db, schema } from '@/lib/db';         // 内部模块
import { Button, Card } from '@/components';   // 组件
import { z } from 'zod';                       // 第三方库
```

### 3.2 React 组件规范

**组件定义**：

```typescript
// Server Component (默认)
// 文件: src/app/projects/page.tsx
import { db } from '@/lib/db/client';

export default async function ProjectsPage() {
  const projects = await db.query.tenderProjects.findMany();
  return <ProjectsList projects={projects} />;
}

// Client Component (需标注)
// 文件: src/components/projects-list.tsx
'use client';

import { useState } from 'react';
import { Project } from '@/types/tender';

interface ProjectsListProps {
  projects: Project[];
}

export function ProjectsList({ projects }: ProjectsListProps) {
  const [selected, setSelected] = useState<string | null>(null);
  return (
    <div>
      {projects.map(project => (
        <ProjectCard
          key={project.id}
          project={project}
          onClick={() => setSelected(project.id)}
        />
      ))}
    </div>
  );
}
```

**组件命名**：
- 页面组件：`XxxPage`
- 列表组件：`XxxList`
- 卡片组件：`XxxCard`
- 表单组件：`XxxForm`
- 对话框组件：`XxxDialog`

**Props 规范**：

```typescript
// 使用 interface 定义 Props
interface DocumentCardProps {
  document: Document;
  onDelete?: (id: string) => void;   // 可选回调
  showActions?: boolean;             // 可选布尔
}

// 解构 Props，使用默认值
export function DocumentCard({
  document,
  onDelete,
  showActions = true
}: DocumentCardProps) {
  // ...
}
```

### 3.3 Server/Client Component 选择

| 场景 | 选择 | 原因 |
|------|------|------|
| 数据获取 | Server | 直接访问数据库 |
| 静态展示 | Server | 无需交互 |
| 用户交互 | Client | 需要 state/event |
| 使用 hooks | Client | useState/useEffect |
| 浏览器 API | Client | window/localStorage |
| 表单处理 | Client | 需要交互状态 |

---

## 4. shadcn/ui 使用规范

### 4.1 组件引入

```typescript
// 从 ui 目录引入
import { Button } from '@/components/ui/button';
import { Card, CardHeader, CardContent } from '@/components/ui/card';
import { Dialog, DialogContent, DialogTrigger } from '@/components/ui/dialog';
```

### 4.2 样式规范

```typescript
// 使用 cn 函数合并样式
import { cn } from '@/lib/utils';

// 使用 class-variance-authority 定义变体
const buttonVariants = cva(
  "inline-flex items-center rounded-md",
  {
    variants: {
      variant: {
        default: "bg-primary text-white",
        outline: "border border-input",
        ghost: "hover:bg-accent",
      },
      size: {
        default: "h-10 px-4",
        sm: "h-9 px-3",
        lg: "h-11 px-8",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
);
```

### 4.3 常用组件

| 组件 | 用途 |
|------|------|
| `Button` | 操作按钮 |
| `Input/Textarea` | 文本输入 |
| `Select` | 下拉选择 |
| `Card` | 卡片容器 |
| `Dialog` | 弹窗对话框 |
| `Toast` | 消息提示 |
| `Badge` | 标签徽章 |
| `Progress` | 进度条 |
| `Tabs` | 标签页切换 |
| `Accordion` | 折叠面板 |

---

## 5. API 开发规范

### 5.1 路由命名规范

```
/api/[module]/[resource]/[action]

示例：
/api/projects                    # 项目列表
/api/projects/[projectId]        # 项目详情
/api/documents/[documentId]/parse # 文档解析
```

### 5.2 请求处理规范

```typescript
// app/api/documents/[documentId]/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { db } from '@/lib/db/client';
import { documents } from '@/lib/db/schema';
import { eq } from 'drizzle-orm';
import { auth } from '@/lib/auth/config';

// GET: 获取资源
export async function GET(
  request: NextRequest,
  { params }: { params: { documentId: string } }
) {
  try {
    const session = await auth();
    if (!session) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    const document = await db.query.documents.findFirst({
      where: eq(documents.id, params.documentId),
    });

    if (!document) {
      return NextResponse.json({ error: 'Not found' }, { status: 404 });
    }

    return NextResponse.json(document);
  } catch (error) {
    console.error('API Error:', error);
    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    );
  }
}

// DELETE: 删除资源
export async function DELETE(
  request: NextRequest,
  { params }: { params: { documentId: string } }
) {
  try {
    const session = await auth();
    if (!session) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    await db.delete(documents).where(eq(documents.id, params.documentId));

    return NextResponse.json({ success: true });
  } catch (error) {
    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    );
  }
}
```

### 5.3 错误处理规范

```typescript
// 统一错误响应格式
{
  "error": "Error message",
  "code": "ERROR_CODE",  // 可选
  "details": {}          // 可选
}

// 状态码规范
200 - 成功
201 - 创建成功
400 - 请求参数错误
401 - 未认证
403 - 无权限
404 - 资源不存在
500 - 服务器错误
```

### 5.4 响应格式规范

```typescript
// 单个资源
{
  "id": "xxx",
  "name": "xxx",
  ...
}

// 资源列表
{
  "data": [...],
  "total": 100,
  "page": 1,
  "pageSize": 20
}

// 操作结果
{
  "success": true,
  "message": "操作成功"
}
```

---

## 6. 数据库开发规范

### 6.1 Schema 变更流程

```
1. 修改 src/lib/db/schema.ts
2. 运行 npm run db:generate
3. 检查生成的 SQL 文件
4. 运行 npm run db:migrate
5. 更新相关代码
```

### 6.2 Migration 管理

```bash
# 开发环境：直接推送
npm run db:push

# 生产环境：生成并执行迁移
npm run db:generate
npm run db:migrate
```

### 6.3 Query 优化建议

```typescript
// 使用索引字段查询
await db.query.documents.findMany({
  where: eq(documents.projectId, projectId), // 有索引
});

// 避免 N+1 查询，使用 relations
await db.query.tenderProjects.findMany({
  with: {
    documents: true,  // 一次性加载关联数据
  },
});

// 分页查询
await db.query.documents.findMany({
  limit: 20,
  offset: 0,
  orderBy: [desc(documents.createdAt)],
});
```

---

## 7. AI 系统开发规范

### 7.1 Agent 开发规范

```typescript
// 新建 Agent
import { Agent } from '@mastra/core/agent';
import { Memory } from '@mastra/memory';
import { pgStore, pgVector } from '@/mastra/storage';

export const newAgent = new Agent({
  id: 'new-agent',
  name: '新智能体',
  description: '...',
  instructions: '...',
  model: reviewModelConfig.defaultModel,
  memory: new Memory({
    storage: pgStore,
    vector: pgVector,
    options: {
      lastMessages: 20,
      workingMemory: { enabled: true, scope: 'resource' },
    },
  }),
  tools: { ... },
});
```

### 7.2 Tool 开发规范

```typescript
// 新建 Tool
import { createTool } from '@mastra/core/tools';
import { z } from 'zod';

export const newTool = createTool({
  id: 'new-tool',
  name: '新工具',
  description: '...',
  inputSchema: z.object({
    projectId: z.string(),
    documentId: z.string().optional(),
  }),
  outputSchema: z.object({
    success: z.boolean(),
    data: z.any().optional(),
  }),
  execute: async ({ context }) => {
    // 实现逻辑
    return { success: true, data: result };
  },
});
```

### 7.3 Prompt 编写规范

- 明确 Agent 角色和职责
- 提供清晰的输入/输出格式
- 说明执行步骤和约束
- 包含错误处理指导

---

## 8. 测试规范

### 8.1 测试类型

| 类型 | 说明 | 工具 |
|------|------|------|
| **单元测试** | 函数/组件测试 | Vitest/Jest |
| **集成测试** | API/模块测试 | Vitest |
| **E2E 测试** | 用户流程测试 | Playwright |

### 8.2 测试命名规范

```typescript
// 测试文件命名
document-parser.test.ts
mineru-client.test.ts

// 测试函数命名
describe('DocumentParser', () => {
  it('should parse PDF successfully', () => { });
  it('should handle parse error', () => { });
});
```

### 8.3 测试覆盖率

- 核心业务逻辑: > 80%
- API 路由: > 60%
- 组件: > 50%

---

## 9. 文档规范

### 9.1 注释规范

```typescript
// 单行注释：说明关键逻辑
const timeout = 300; // MinerU 解析超时（秒）

// 多行注释：说明复杂逻辑
/**
 * 处理文档解析结果
 * 1. 检查解析状态
 * 2. 保存 blocks 到数据库
 * 3. 更新文档状态
 */
function handleParseResult(result: ParseResult) { }

// 不写注释的情况：
// - 代码逻辑清晰
// - 变量/函数名已说明用途
// - 类型定义已说明结构
```

### 9.2 文件头部注释

```typescript
/**
 * 文档解析客户端
 *
 * 功能：
 * - 提交文档解析任务到 MinerU
 * - 查询解析状态和结果
 * - 处理异步任务轮询
 *
 * 配置：
 * - MINERU_API_URL: MinerU 服务地址
 * - MINERU_TIMEOUT: 请求超时时间
 */
```

---

## 10. 安全规范

### 10.1 输入验证

```typescript
// 使用 Zod 验证输入
import { z } from 'zod';

const documentSchema = z.object({
  name: z.string().min(1).max(255),
  docType: z.enum(['tender_doc', 'legal_doc', 'bid_doc']),
  fileSize: z.number().positive().max(100 * 1024 * 1024), // 最大 100MB
});

// API 中验证
const body = await request.json();
const validated = documentSchema.parse(body);
```

### 10.2 权限验证

```typescript
// API 中验证用户权限
const session = await auth();
if (!session) {
  return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
}

// 验证组织权限
const membership = await db.query.organizationMembers.findFirst({
  where: eq(organizationMembers.userId, session.user.id),
});

if (!membership || membership.role !== 'admin') {
  return NextResponse.json({ error: 'Forbidden' }, { status: 403 });
}
```

### 10.3 防止常见漏洞

| 漏洞 | 防护措施 |
|------|---------|
| **SQL 注入** | 使用 Drizzle ORM，不拼接 SQL |
| **XSS** | React 自动转义，使用 dangerouslySetInnerHTML 时谨慎 |
| **CSRF** | NextAuth 内置保护 |
| **文件上传** | 验证文件类型、大小、内容 |