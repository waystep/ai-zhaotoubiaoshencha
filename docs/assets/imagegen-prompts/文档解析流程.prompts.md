# 文档解析流程配图提示词

## document-parse-workflow.png

- 目标文档: `docs/workflows/文档解析流程.md`
- 插入小节: `4.2 解析流程图`
- 用途: 展示解析任务提交、MinerU 异步处理、获取结果、更新状态的闭环。
- 最终 prompt:

```text
Use case: infographic-diagram
Asset type: technical documentation workflow image
Primary request: Create a 16:9 asynchronous document parsing workflow overview.
Scene/backdrop: white engineering documentation canvas with subtle gray grid.
Subject: six-step flow: Submit task, MinerU API server, Async parser, Parse complete, Fetch results, Update status. Show a document entering the pipeline and parsed blocks plus extracted images leaving the pipeline.
Style/medium: polished flat workflow infographic, crisp vector-like bitmap.
Composition/framing: horizontal process with a return/status loop, balanced spacing.
Lighting/mood: bright, precise, operational.
Color palette: white, light gray, blue and teal accents, green completion marker.
Text (verbatim): "提交任务", "MinerU", "异步解析", "解析完成", "获取结果", "更新状态"
Constraints: use Chinese for all non-technical labels, keep only established technical terms in English such as MinerU; short readable labels only, no long paragraphs, no logos, no watermark.
Avoid: photorealistic screenshots, dark theme, tiny unreadable text, excessive arrows.
```

## document-image-storage.png

- 目标文档: `docs/workflows/文档解析流程.md`
- 插入小节: `6.2 图片保存`
- 用途: 展示 MinerU 提取图片从 base64 到本地目录，再通过 API 被访问的保存流程。
- 最终 prompt:

```text
Use case: infographic-diagram
Asset type: technical documentation workflow image
Primary request: Create a 16:9 image extraction and storage workflow overview for parsed document images.
Scene/backdrop: clean white technical documentation canvas with subtle grid.
Subject: flow from MinerU image blocks to base64 decode, save into document image folder, store metadata, serve through image API. Include simple folder, image thumbnail, database, and API route icons.
Style/medium: flat technical infographic, crisp vector-like bitmap.
Composition/framing: left-to-right storage pipeline with a small directory tree visual.
Lighting/mood: bright, organized, implementation-focused.
Color palette: white, light gray, blue, teal, small green success accents.
Text (verbatim): "MinerU", "Base64", "图片保存", "元数据", "API", "访问"
Constraints: use Chinese for all non-technical labels, keep only established technical terms in English such as MinerU, Base64, API; short readable labels only, no code snippets, no logos, no watermark.
Avoid: realistic file paths with tiny text, dark theme, clutter, unreadable labels.
```
